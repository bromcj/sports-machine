# Runtime. Lower bounds, not pins: this is a small project and the cloud job
# reinstalls on every run, so a hard pin would age into a broken build faster
# than it would prevent one. The CI workflow runs the test suite on every push,
# which is what actually catches a bad upgrade.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
requests>=2.31
pybaseball>=2.2

# Every parquet read and write goes through this. pandas does not pull it in,
# so without it `python backfill.py` and every feature build fail on a fresh
# checkout - it was only ever working because it arrived as somebody else's
# transitive dependency.
pyarrow>=14.0
