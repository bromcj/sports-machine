# Data stays local — never push to a public repo
data/
logs/
*.db
*.parquet

# Python junk
__pycache__/
*.pyc
.venv/

# Trained models are rebuilt by the training scripts
data/models/

# Regenerated on every monitor run; tracking it would dirty the
# working tree and make machine_daily.bat refuse to pull.
ALERTS.md

# Generated dashboard
dashboard.html
dashboard.png
data_phase1/
data_golden/
